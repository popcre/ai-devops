{
  "description": "Providers in config/reviewer-registry.json that review outside the shared-db allocator rotation, so bin/ai-reviewer-membership-drift does not compare them.",
  "outside_allocator": {
    "claude": "Approval-gate reviewer, not drawn by the allocator.",
    "codex": "Overflow one-shot reviewer, not drawn by the allocator."
  }
}
