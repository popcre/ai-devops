interface:
  display_name: "Shared DB Orchestrator"
  short_description: "Coordinate safe shared database work"
  default_prompt: "Use $shared-db-orchestrator to coordinate this shared-db structure change safely."
