# Smart App Control reconciliation — offline verification

Verified on Windows against the exact pull-request source after rebasing onto
the Kimi fixture repair.

- `pwsh -NoProfile -File tests/test-reconcile-smart-app-control.ps1`: all
  failure, retry, idempotency, backup, refresh-marker, and TestOnly fixtures
  passed. A forced `CiTool.exe -r` failure stayed noncompliant; retry refreshed
  policy and wrote durable compliance evidence; a second run created no backup.
- `pwsh -NoProfile -File tests/windows-winget-config.tests.ps1`: passed the
  non-installing WinGet/DSC structural suite.
- `bash tests/test-windows-scripts.sh`: **47 passed, 0 failed**.
- `bash tests/test-workflow-policy.sh`: passed, including the 76 Bash / 19
  PowerShell manifest totals and complete Windows-matrix ownership checks.

These are offline safety proofs. Issue #200 remains open until the merged source
is installed on the named runner, `CiTool.exe -r` succeeds there, the runner
service remains running, and the clean disposable Windows 11 acceptance proof
exists.
