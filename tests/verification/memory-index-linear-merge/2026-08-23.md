# Memory index linear-merge verification — 2026-08-23

Candidate scope: `bin/ai-sync-memory` and `tests/test-ai-memory-sync.sh`.

Run from `C:\repos\ai-devops` with the repository-required Git Bash:

| Check | Result |
|---|---|
| `bash tests/test-ai-memory-sync.sh` | PASS — private memory sync converges losslessly and preserves every failure state |
| `bash tests/test-ai-sync-memory-public-containment.sh` | PASS — public memory uploads are blocked and pull preserves both indexes |
| `bash -n bin/ai-sync-memory` | PASS |
| `pwsh -NoProfile -File tests/test-memory-sync-scheduled-task.ps1` | PASS — Windows memory sync scheduled task is hidden and bounded |
| `bash tests/test-windows-scripts.sh` | PASS — 40 passed, 0 failed |

The memory suite includes a 20,000-entry CRLF merge bounded to 15 seconds,
zero-byte hub and machine indexes, and all four interrupted scratch-file names.
It was rerun after the concurrent `origin/main` reconciliation and passed.
