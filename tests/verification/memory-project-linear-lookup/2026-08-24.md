# Memory project lookup verification — 2026-08-24

## Production-like reproduction

- Machine: ALBT16 (Windows 11, Git Bash).
- Local Claude project memory directories matched 737 historical reviewer
  sandboxes.
- The pre-fix pull completed only 13 reviewer-sandbox projects before the
  rollout was safely interrupted. The nested hub-project by local-project scan
  required more than 543,000 repeated classifications for 737 projects.
- The private hub had already preserved divergent state. The interrupted pull
  left no sync process and no lock directory, so the lossless, idempotent pull
  can be rerun safely.

## Fix and evidence

`bin/ai-sync-memory` now loads explicit aliases once, indexes local memory paths
once, and directly looks up every hub project. It preserves multiple local
checkout paths for one logical project, first-declaration-wins aliases, CRLF
maps, and a final map line without a newline.

| Check | Result |
|---|---|
| `bash -n bin/ai-sync-memory` | PASS |
| `bash tests/test-ai-memory-sync.sh` | PASS — private memory sync converges losslessly and preserves every failure state |
| `bash tests/test-ai-sync-memory-public-containment.sh` | PASS — public uploads remain blocked and both indexes are preserved |
| 500 local by 500 hub lookup regression | PASS within the test's 30-second Windows bound, with two local checkouts mapped to one project |

The complete Linux and Windows suites remain the exact-head GitHub release
gate before this change is installed on T16 or promoted to production.
