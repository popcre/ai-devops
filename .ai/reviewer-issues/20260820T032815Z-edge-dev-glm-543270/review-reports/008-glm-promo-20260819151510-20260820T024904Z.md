# GLM review — promo-20260819151510

| field | value |
|---|---|
| model | zai-coding-plan/glm-5.3 |
| opencode | 1.18.12 (pinned) |
| session | promo-20260819151510 |
| session id | `ses_fe2f6157dffejN97Lr3zako70L` |
| repository | C:/repos/shared-db |
| branch | main |
| caller | claude |
| requested | 2026-08-20T02:49:04Z |
| tokens | `{"input":253,"output":2738,"reasoning":3511,"cache":{"read":152512,"write":0}}` |

---

# Independent review — production promotion of `20260819151510_wildbrain_nbcu_licensing_read_access.sql`

## COVERAGE STATEMENT

**Migration under review — read in full, all 364 lines** (`supabase/migrations/20260819151510_wildbrain_nbcu_licensing_read_access.sql`): lines 1–200 and 201–364, i.e. the header block, all 28 grant+policy pairs (lines 82–220), and the complete `do $$` assertion block (lines 229–364).

**Packet files read in full:** `.ai-review/MANIFEST.md`, `.ai-review/patch.diff`.

**Other files read (forward-only verification of pre-existing and inherited state):**
- `20260819014639_wildbrain_dam_source_landing.sql` — RLS/grant/policy block (≈1314–1426)
- `20260810070000_nbcu_creative_asset_factory_landing.sql` — RLS/grant block (≈1113–1165) plus header greps
- `20260811070000_nbcu_asset_ip_family_relationship.sql` — RLS/grant block (≈186–209, 474–565)
- `20260810080000_nbcu_revoke_default_granted_write_privileges.sql`, `20260810180000_plm_default_privilege_hole_and_pg17_maintain_revokes.sql` — relevant greps only
- `20260819015333_sega_dsi_source_landing.sql` — grant/policy block (1282–1439), the reference predicate verified verbatim at 1297–1298
- `20260819125713_peanuts_tenovos_source_landing.sql` — 1690–1705
- `20260810190000_dcp_vault_source_landing.sql` — 1378–1391
- `20260621150815_app_core.sql` — 355–429 (`app.has_role` / `has_any_role` / `has_app_access` definitions; the only definitions anywhere in `supabase/migrations`)
- `20260621150714_foundation.sql` — 35–54 (`app.jwt_role_names`)
- `20260819151527`, `20260819151536`, `20260820004338` — grepped for any grant/policy/alter on the 28 tables: none

Not verified by me (no DB/network access in this profile): the production ledger reading of 2026-08-20T02:33Z and recovery run 32325276117. I take the orchestrator's statements as given.

## FINDING 0 — Medium (packet defect, not the migration)

The evidence packet does not contain the change under review. `.ai-review/patch.diff` is the `scripts/manage-migration-author-lanes.mjs` reviewer-rotation change (base `55f0eb7` → head `938870d`), while the packet tag says `glm-promo-20260819151510`. I reviewed the migration file itself from the checkout, which is the object named in the decision. A verdict recorded against this packet would cite evidence for a different change — the wrapper should fix its diff selection, and the orchestrator should attach this review to the migration path, not the manifest's diff.

## RISK 1 — APPLY-TIME COST

**(a) No apply-time statement reads, counts, scans or joins ROWS of any user table. There are none.** The complete statement inventory: 28 `grant select … to authenticated` (lines 82–219), 28 `create policy … for select to authenticated` (83–220), one `do` block (229–364). Every query in the do block is catalogue-only:
- `pg_policies` — reference reads at **:256** and **:267**; per-table shape/qual reads in the loop (~:303, ~:312, ~:322, ~:331)
- `has_table_privilege('authenticated', …)` — **:277**, an ACL check, no heap touch
- `information_schema.role_table_grants` — **:285**, **:295**
- `pg_class` join `pg_namespace` for `relrowsecurity` (~:340)

This is categorically different from the 20260819151536 failure, whose verify block did data work that was trivial on near-empty preview. Locks: `grant`/`create policy` each take a momentary AccessExclusiveLock on the target table — the identical lock class `20260819014639:1330–1416` already took on the same tables; not a scan, not a stall risk.

**(b) Yes — every assertion is catalogue-based.** `pg_policies`, `pg_class`/`pg_namespace`, `information_schema.role_table_grants`, `has_table_privilege`. No assertion counts or samples rows. This is the required pattern.

**(c) Bounded, not quadratic.** Fixed 28-element array (asserted = 28 at ~:244), ~7 catalogue lookups per iteration ≈ 200 equality-filtered catalog queries total. The `role_table_grants` view pattern is production-proven: `20260811070000:553–565` and `20260810080000:114–125` assert through the same view and both applied to production.

**Finding — Medium (promotion precondition, invisible to the static guard).** The do block hard-depends on **already-applied** objects: `plm.sega_capture_plm_read` and `plm.sega_capture_service_read` (**:256, :267**), which exist only if `20260819015333` is applied; and the 28 target tables, which need `20260819014639`, `20260810070000`+`20260810080000`, and `20260811070000` applied in order. Per §5.1-A, `strip_sql` removes dollar-quoted bodies by design, so the guard chain **cannot see this dependency** — "PREFLIGHT OK" says nothing about it. If 151510 is promoted alone onto a production lacking sega, the file aborts at `policy sega_capture_plm_read is missing` — cleanly, per the #611 per-file atomicity finding, but it is a wasted apply. **Precondition for promotion: prove from the full production ledger (§4.3 discipline — full list comparison, never high-water marks) that those five prerequisite versions are applied, or include them in the same bounded allowlist in dependency order.** The orchestrator's 2026-08-20T02:33Z `supabase migration list` output can answer this directly.

## RISK 2 — THE SECURITY CHANGE

**(d)** From the SQL, not the summary: exactly one principal gains anything. `authenticated` gains **SELECT only** on the 28 tables — 11 `plm.wildbrain_{capture, era, creative_group, asset_category, asset_nature, character, asset, asset_character, guide, guide_alias, asset_guide}` (lines 82–132) and 17 `plm.nbcu_{capture, right, scope, property, ip_family, character, style_guide, asset, asset_metadata_value, asset_scope, ip_family_property, property_character, asset_property, asset_character, asset_style_guide, style_guide_property, asset_ip_family}` (139–219). Assertion 2 (:285–292) fails the file if `authenticated` holds any non-SELECT privilege on any of them. `service_role` grants are untouched; `anon`/`PUBLIC` receive nothing and are asserted to hold nothing (:295–301).

**(e)** Quoted (identical at all 28, e.g. :83–84):

```sql
for select to authenticated using (app.has_app_access('plm') or app.has_role('administrator') or app.has_any_role(array['sales', 'licensing']::app.app_role[]))
```

No policy reduces to `true`. From the current function definitions (`20260621150815:365–410` — the only definitions in the tree; no later `create or replace` exists), the admitted population is **administrators ∪ holders of an unrevoked `app.app_access` row for `plm` ∪ sales ∪ licensing**, each satisfiable either by `app.user_role` or by the JWT `app_metadata.roles` arm (`20260621150714:35–53`). So it is not literally "Licensing only" — sales and PLM users are admitted — but that is the established house predicate, not a widening smuggled in: it is character-identical to the applied Sega predicate (`20260819015333:1297–1298`, verified verbatim), the Peanuts predicate (`20260819125713:1690–1691`) and the Disney DCP predicate (`20260810190000:1383–1388`), and assertion 5 (~:310–317) fails the file if any of the 28 stored quals differs from Sega's **stored** qual. WildBrain/NBCU becomes readable by exactly the population that already reads Sega, Peanuts and Disney vault data.

**(f)** `anon` is excluded **at the grant**, which is the binding control: the file's only grantee is `authenticated`; the pre-existing state had already revoked all from `anon` and `PUBLIC` on all 28 tables (`20260819014639:1331–1332`, `20260810070000:1143–1144`, `20260811070000:196–197`); assertion 3 (:295–301) reads back `role_table_grants` for grantee in (`anon`,`PUBLIC`) and fails on any row. The policies are `to authenticated`, not `to public`, so the policy layer offers anon nothing either. Both layers align. Additionally `plm` is not in production `pgrst.db_schemas` (§8.1), so none of this is browser-reachable through PostgREST at all — the exposure is to direct-DB credentials whose JWT resolves into the admitted population.

**(g)** No existing policy or grant on any pre-existing object is altered, replaced or dropped. The file contains no `drop policy`, `alter policy`, `revoke`, `alter table`, or `create or replace function` — repo-wide greps confirm no migration anywhere drops or alters a policy on a `plm.wildbrain_*`/`plm.nbcu_*` table. The 28 pre-existing `<table>_service_read` policies (`for select to service_role using (true)`) are untouched and their survival — name, command, role **and stored expression** — is asserted (checks 6a/6b, ~:320–336). The Disney OPA policy corrected by `20260807190000` is not referenced by any executable statement here.

**(h)** No leak beyond the approved audience. The newly readable tables are licensed portal content, and the audience is byte-identical (by assertion) to the audience that already reads the Sega, Peanuts and Disney vault landing tables. Two caveats, both Low: (1) I verified sega/peanuts/dcp predicates line-by-line; the header's claim about `wb_*` I did not — immaterial, since the assertion compares against Sega. (2) `plm.nbcu_right` gains read here while AGENTS.md §6.16 orders that table dropped; it has never been loaded, so the grant exposes nothing, and the future `DROP TABLE` will carry the policy away — untidy, not unsafe.

## Remaining findings

- **Low (informational)** — the JWT arm of `has_role` admits anyone whose token carries the slug in `app_metadata.roles`, independent of DB role rows. Pre-existing house behaviour on every `plm` landing schema; not introduced or widened here.
- **Low (informational)** — I did not re-run the behavioural matrix (54/0) and the packet itself ran no tests; the static analysis above is consistent with the described outcomes.

## Summary

The migration is exactly what it claims to be: 28 additive grants plus 28 additive policies, SELECT-only, house-predicate-gated, anon-refused, nothing narrowed, with a self-verifying catalogue-only assertion block that is cheap on any database size. The one real risk is promotion mechanics, not the file: a do-block dependency the static guard cannot see.

VERDICT: APPROVE
